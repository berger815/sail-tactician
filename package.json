{
  "name": "sail-tactician",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/verify-core-browser.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:structure": "node --input-type=commonjs --check < src/legacy/runtime-state.js && node --input-type=commonjs --check < src/legacy/app-1.js && node --input-type=commonjs --check < src/legacy/app-2.js && node --input-type=commonjs --check < src/legacy/app-3.js && node scripts/validate-structure.mjs",
    "check": "npm run check:structure && npm run test && npm run build"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
